Unlock after Lock no longer depends on unwrapping the encrypted user key (which often returned null and falsely reported a wrong password). The master-password hash stored at login is checked instead, matching how sign-in already treated a missing user key.
