Kept the vault unlocked across refresh by storing extractable encryption keys in sessionStorage (same tab lifetime as the auth session). Explicit Lock still clears keys; closing the tab clears them too.
