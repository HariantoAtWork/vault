# Fixed self-hosted server selection and URL input on login

Selecting Self-hosted threw when `serverConfig` tried to resolve an empty URL, which broke the login form.

Server URL resolution is now safe while typing, server selector uses native buttons with proper v-model binding, and the self-host field accepts full URLs like `https://vault.sylo.space`.
