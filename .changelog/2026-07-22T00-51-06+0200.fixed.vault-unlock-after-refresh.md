After a refresh the session token survived but encryption keys did not, so the vault rendered empty and odd. Added a lock screen to re-enter the master password, skipped SSR auth redirects for sessionStorage sessions, and split Lock vs Sign out.
